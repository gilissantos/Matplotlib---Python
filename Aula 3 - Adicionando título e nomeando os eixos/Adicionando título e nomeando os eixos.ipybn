#As funções title(), xlabel() e ylabel() recebem parâmetros do tipo de dados strings, ou seja, tipo texto. No exemplo que vamos ilustrar não necessita de dados ou de um dataset.

%matplotlib inline
import matplotlib.pyplot as plt


#Vamos realizar uma plotagem sem dados conforme citado acima:


plt.plot()
plt.title("Título do Gráfico")
plt.xlabel("Eixo X")
plt.ylabel("Eixo Y")
plt.show()